# Snow CLI runtime state
.snow/

# Local working config (contains machine-specific absolute paths)
config/config.json
config/config.json.bak
*.bak

# Archive (retired/version history binaries, kept local only)
archive/*.zip

# Build artifacts
artifacts/
