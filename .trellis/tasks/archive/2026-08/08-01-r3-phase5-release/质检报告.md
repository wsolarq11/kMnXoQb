# R3 迁移质检报告（实战实测验真质检）

日期：2026-08-01。任务：r3-phase5-release。
质检原则：真实数据、真实进程、真实场景；自动化项附证据，人工项标注待执行。

## 1. 产物与构建链（PASS，自动化）

| 项 | 结果 | 证据 |
|---|---|---|
| release exe | PASS | 9.5MB，`tauri build --no-bundle`（cargo 1.97，lto+strip 默认） |
| MSI（per-user WiX） | PASS | `launchpad-tauri_0.1.0_x64_en-US.msi`（3.2MB）；WiX 工具下载曾超时，手动放入 `%LOCALAPPDATA%\tauri\WixTools314` 后构建成功 |
| Portable zip | PASS | `Launchpad-0.1.0-Windows-Portable.zip`（2.8MB：exe + launchpad.portable 标记 + README）；**注意 zip 输出目录不能用 vite 的 dist/**（beforeBuildCommand 清空），脚本用 release/ |
| 测试全绿 | PASS | cargo 119（含真实 spawn 契约 15s）+ vitest 16；零警告 |

## 2. 便携形态（PASS，自动化）

| 场景 | 结果 | 证据 |
|---|---|---|
| 解压即用 | PASS | zip 解压到空目录 → exe 启动 → 进程存活 + WebView2 渲染 |
| exe 旁 config/ 自动生成 | PASS | 首次运行自动创建 config/ 目录（ConfigStore 构造时 CreateDirectory） |
| 真实配置读取 | PASS | 仓库 config/config.json + settings.json 复制到便携目录 → 应用启动加载渲染（截图 quality-portable-realdata.png） |
| 可移动性（向上搜索） | PASS | 阶段 0 已验证：exe 在深层目录时向上命中祖先的 config/（修复后返回 config/ 子目录，与 C# ResolveConfigDir 语义一致） |
| WebView2 冷启动 | 注意 | LTSC 环境首次 ~35s（Runtime 150 初始化）；常驻使用无感 |

## 3. 真实启动契约（PASS，自动化——cargo 契约测试）

- pwsh fallback 在 6 类目录（plain/空格/引号/分号/&/中文目录）真实 spawn，`cd '<转义目录>'; Write-Output MARKER` 输出正确（terminal_contract.rs，15.16s）
- cmd fallback `/k <cmd>` + WorkingDirectory 传递（引号陷阱规避）验证通过
- wt 路径：本机装有 Windows Terminal 1.24；planner 探测逻辑契约覆盖（有 wt 用 wt，无 wt 回退）
- 错误码契约：缺目录 → 267（ERROR_DIRECTORY）；缺可执行 → 2（Rust std 丢失 Win32 码，spawner 补 NotFound→2 映射）

## 4. 中文（部分 PASS，IME 交互为人工项）

| 项 | 结果 | 证据 |
|---|---|---|
| 中文目录端到端 | PASS | 契约测试含"中文目录"用例 |
| 中文界面渲染 | PASS | 窗口/界面中文正常（截图）；中英键表 62 键双语言完整性测试 |
| 语言切换热刷新 | PASS（代码路径） | toggle_language → 核心三态循环 → 前端重渲染；vitest 覆盖 store 流转 |
| **编辑框中文 IME 输入** | **人工项（待执行）** | WebView2 系统级输入法（Chromium），阶段 0 中文回传验证通过；请在真实机器上实测候选/组词/上屏 |

## 5. 场景走查（人工项，待执行）

以下场景需在真实 UI 中人工走查（本任务无法自动化 GUI 交互）：
- [ ] 新建/编辑/删除/上移/下移条目（含校验错误提示）
- [ ] 搜索过滤（名称/目录/命令，不区分大小写）
- [ ] 多选 + 全选 + 批量启动（含危险项确认框、部分失败状态文案）
- [ ] 单项启动确认流程（危险命令三处警告：编辑框/卡片/确认框）
- [ ] 主题三态循环（system/dark/light）
- [ ] 窗口状态保存/恢复（移动窗口 → 重启 → 位置还原；最小化坐标纠偏）
- [ ] 单实例（第二实例退出）
- [ ] config.json 损坏 → 自动从 .bak 恢复 + 状态栏提示
- [ ] 目录选择器（系统 IFileDialog）

## 5.5 MSI 安装形态实测（PASS，自动化——2026-08-01 补充）

| 项 | 结果 | 证据 |
|---|---|---|
| per-user 安装位置 | PASS | 装到 %LOCALAPPDATA%\Programs\launchpad-tauri\（exe + dll + 卸载快捷方式）；Program Files 无安装 |
| 安装版运行 | PASS | 进程存活 + WebView2 渲染；%APPDATA%\launchpad\config\ 自动创建 |
| 卸载 | PASS | msiexec /x 干净卸载（exit 0）；%LOCALAPPDATA%\Programs 无残留；注册表卸载项清除 |
| 用户数据保留 | PASS | 卸载后 %APPDATA%\launchpad\config 保留（设计语义：卸载不删配置） |
| 遗留修复 | 已修 | 默认模板是 perMachine + ProgramFilesFolder（初版装到 C:\Program Files）→ 自定义 per-user 模板（LocalAppDataFolder\Programs + InstallPrivileges=limited + HKCU KeyPath + ICE64 RemoveFile）落地并实测通过 |

## 5.6 命令层全链路测试（PASS，自动化——2026-08-01 补充）

`tests/commands_flow.rs`（6 个测试）：通过 `*_impl` 实现函数驱动 18 个命令在真实 AppState（真实 ConfigStore + 真实 spawner/detector）上的完整行为：

| 覆盖 | 断言 |
|---|---|
| CRUD 全流程 | 创建（id 生成）/列表/更新/移动/选择/删除，磁盘持久化 round-trip |
| 危险确认策略 | 全局开关关→不确认（危险命令仍返回 danger_key）；开→需确认 |
| 启动失败路径 | 目录缺失→WorkingDirectoryMissing 结构化错误；历史不更新（成功才入历史） |
| 批量全失败 | succeeded=0 + 失败索引 [0,1]；批量后选择清空（legacy） |
| 设置循环 | 主题 system→dark→light→system；语言 auto→zh-CN→en-US→auto；get_language 三层优先级 |
| 选择幂等 | target 态幂等（重复 set 不翻转） |

实现说明：commands 重构为  +  薄壳（State 解包）——可测性提升，前端调用无感知。启动成功路径由契约测试覆盖（避免测试弹终端窗口）。
测试总数：cargo 125 + vitest 16 = **141 全绿**，零警告。

## 6. 已知缺口与后续（预备后续 phase 输入）

| # | 缺口 | 说明 |
|---|---|---|
| 1 | MSI 安装形态未实测 | 本机安装会改系统状态；建议在干净机器/虚拟机验证：安装、%APPDATA%\launchpad\config 生成、卸载残留 |
| 2 | 删除确认对话框走查 | DeleteDialog 已实现（store: deleteTarget → 确认 → deleteItem），人工项 |
| 3 | 窗口恢复的 Tauri 侧接线 | 前端 restoreWindow 调 `__TAURI__.window`（best-effort）；正式接线建议用 Rust 侧 window 事件（阶段 6 候选） |
| 4 | 自动更新 | P2（Tauri updater 生态预留，未启用） |
| 5 | Mica/Acrylic 背景 | P2（当前纯色 + 系统主题变量） |
| 6 | CI 发布流水线 | 当前手动构建；github actions 双锁文件缓存键就绪（候选） |
| 7 | 图标 | lucide-react 已用（约束满足）；字体图标非必需 |

## 7. 结论

自动化质检全项 PASS；人工场景清单已列出（第 5 节），建议在真实使用中逐项勾选。产物可交付：`launchpad-tauri/release/Launchpad-0.1.0-Windows-Portable.zip` + `src-tauri/target/release/bundle/msi/launchpad-tauri_0.1.0_x64_en-US.msi`。
